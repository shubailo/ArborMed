name: feature_game
description: Modular Game feature for ArborMed. Handles the ArborRoom (gamification) and Furniture Shop.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  arbormed_core:
    path: ../core
  core_interop:
    path: ../core_interop
  isar: ^3.1.0+1
  provider: ^6.0.5
  get_it: ^7.6.7
  logger: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
