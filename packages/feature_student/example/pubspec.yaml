name: feature_student_example
description: Example app for testing feature_student in isolation.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  feature_student:
    path: ../
  arbormed_core:
    path: ../../core
  core_interop:
    path: ../../core_interop
  provider: ^6.0.5
  get_it: ^7.6.7
  go_router: ^13.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
