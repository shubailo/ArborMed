import 'package:flutter/material.dart';

class StudentDashboardScreen extends StatelessWidget {
  const StudentDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // This is a placeholder that will eventually contain the modular RoomWidget
    // For now, it's a shell to establish the package structure
    return Scaffold(
      appBar: AppBar(
        title: const Text('Student Dashboard'),
      ),
      body: const Center(
        child: Text('Modular Student Dashboard coming soon...'),
      ),
    );
  }
}
