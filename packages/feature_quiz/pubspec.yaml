name: feature_quiz
description: Core quiz engine and session management for ArborMed.
version: 0.1.0+1
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Internal Packages
  core_interop:
    path: ../core_interop
  arbormed_core:
    path: ../core

  # Utilities
  provider: ^6.1.1
  get_it: ^7.6.6
  go_router: ^13.0.0
  confetti: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
