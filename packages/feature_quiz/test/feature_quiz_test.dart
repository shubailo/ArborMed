import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Quiz Feature module loads without export errors', () {
    // Basic test to verify structure and package imports
    expect(true, isTrue);
  });
}
