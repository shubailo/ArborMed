name: feature_auth_example
description: Example application for testing the feature_auth package.
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  feature_auth:
    path: ..
  arbormed_core:
    path: ../../core
  core_interop:
    path: ../../core_interop
  provider: ^6.0.5
  go_router: ^13.0.0
  get_it: ^7.6.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
