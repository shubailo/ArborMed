1. **Remove Firebase Initialization or Make it Conditional**
   The issue is that Firebase is being initialized with empty options (from environment variables that are missing when building normally) which causes a fatal crash on startup, hanging the app on the splash screen. We need to wrap `Firebase.initializeApp()` in a `try/catch` block or check if we have a valid configuration before attempting initialization in `main.dart`. I will check if `options.apiKey` is not empty. Wait, the code reviewer said that the problem is that we swallow the exception. Is Firebase optional or mandatory? If it is optional, we shouldn't fail. However, we could just check if the `DefaultFirebaseOptions.currentPlatform.apiKey.isEmpty`. If it is empty, we print a warning and skip `Firebase.initializeApp`.
