## 2026-04-23 - Add localized tooltips to icon-only action buttons
**Learning:** Icon-only action buttons without tooltips lack proper accessibility and screen reader support. Using project-specific localization strings allows for standard navigation.
**Action:** When working on apps, verify all icon-only `IconButton`s include `tooltip` properties mapped to localized string values.
